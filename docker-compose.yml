version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: elsermanuel/elsermanueldocker
    ports:
      - "8085:8085"
    environment:
      - DATABASE_URL=${URL_MONGO}
      - API_KEY=${URL_CLOUD}

